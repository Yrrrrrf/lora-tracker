[project]
name = "lora-tracker"
version = "0.1.0"
description = "Lora Tracker Dashboard Application for LoRaWAN devices"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pyserial>=3.5",
    "requests>=2.32.3",
]
